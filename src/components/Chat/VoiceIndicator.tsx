import { useVoiceStore } from "../../store/voiceStore";

export function VoiceIndicator() {
  // All voice status indicators have been moved to InputBar.tsx
  // This component is kept for future use if needed
  return null;
}
